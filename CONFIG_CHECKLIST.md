# Cyberboss 配置清单

> 请填写以下信息，完成后告诉我，我会读取并帮你配置。

---

## 1. 基本信息（必填）

| 配置项              | 说明                        | 你的值     |
| ------------------- | --------------------------- | ---------- |
| 你的名字            | 用于生成 persona 人格       | `小方`     |
| 你的性别            | `male` / `female` / `other` | `male`   |
| 你的 WeChat 用户 ID | 用于访问控制                | `chzxk666` |

---

## 2. 工作目录（必填）

| 配置项     | 说明                                | 你的值                       |
| ---------- | ----------------------------------- | ---------------------------- |
| 项目根目录 | 你希望 Cyberboss 绑定到哪个文件夹？ | `D:\Cyberboss`） |

---

## 3. 运行时选择

你使用 **Claude Code**，所以运行时是 `claudecode`。

| 配置项        | 说明                                                                               | 你的值   |
| ------------- | ---------------------------------------------------------------------------------- | -------- |
| Claude 模型   | 不填则用默认。可选：`sonnet` / `opus` / `haiku` / `fable`                          | `______` |
| 权限模式      | Claude Code 的权限模式，如`default` / `acceptEdits` / `plan` / `bypassPermissions` | `default` |
| 额外 CLI 参数 | 逗号分隔，如`--fast,--verbose`                                                     | `______` |

---

## 4. 视觉/图片处理

你提到没有视觉模型，建议设为 `off`。

| 配置项   | 可选值                                | 你的值        |
| -------- | ------------------------------------- | ------------- |
| 视觉模式 | `auto` / `caption` / `native` / `off` | `off` ← 建议 |

如果是 `off`，下面的视觉 API 配置都**不需要**填写。

---

## 5. 位置服务（可选）

是否需要启用手环/手机位置上报？

| 配置项       | 说明                   | 你的值   |
| ------------ | ---------------------- | -------- |
| 启用位置服务 | `true` / `false`       | `false` |
| 位置 Token   | 自定义一个Bearer Token | `______` |
| 家坐标       | `lat,lng` 格式         | `______` |
| 公司坐标     | `lat,lng` 格式（可选） | `______` |

---

## 6. WeChat 机器人

| 配置项                     | 说明                                      | 你的值    |
| -------------------------- | ----------------------------------------- | --------- |
| 是否已完成微信机器人注册？ | 需要先在微信开放平台创建机器人            | `是 ` |
| 如果有 Account ID          | 已登录的账号 ID（先不填，登录后自动生成） | `______`  |

---

## 7. 确认清单

请确认以下项并告诉我：

- [ ] Node.js >= 22 已安装 ✅（检测到 v24.15.0）
- [ ] Claude Code CLI 已安装 ✅（检测到 `/c/Users/29601/.local/bin/claude`）
- [ ] npm install 已完成 ✅（检测到 node_modules）
- [ ] **第1项：请填写性别**
- [ ] **第2项：请确认工作目录**
- [ ] **第3项：请选择 Claude 模型（可选）**
- [ ] **第4项：确认 vision mode = off？**
- [ ] **第5项：是否需要位置服务？**
- [ ] **第6项：微信机器人是否已注册？**

---

## 快速回复模板

你可以这样回复我（修改冒号后的内容）：

```
性别：male
工作目录：D:\Cyberboss
Claude模型：默认就好
权限模式：default
视觉模式：off
位置服务：暂时不用
微信机器人：还没注册，需要帮助
```

